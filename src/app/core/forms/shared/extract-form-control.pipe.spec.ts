import { ExtractFormControlPipe } from './extract-form-control.pipe';

describe('ExtractFormControlPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractFormControlPipe();
    expect(pipe).toBeTruthy();
  });
});
