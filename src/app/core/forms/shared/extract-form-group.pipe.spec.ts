import { ExtractFormGroupPipe } from './extract-form-group.pipe';

describe('ExtractFormGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractFormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
